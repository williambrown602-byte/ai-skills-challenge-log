name: Generate Daily Challenge

on:
  schedule:
    # Runs at 4:30 PM London time (16:30 UTC in winter, 15:30 UTC in summer)
    # Using 16:30 UTC - will be 4:30 PM GMT or 5:30 PM BST
    - cron: '30 16 * * *'
  workflow_dispatch:  # Allows manual trigger

jobs:
  generate-challenge:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          pip install anthropic

      - name: Generate daily challenge
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: python generate_challenge.py

      - name: Commit and push changes
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add .
          git diff --staged --quiet || git commit -m "Add daily challenge for $(date +'%B %d, %Y')"
          git push
